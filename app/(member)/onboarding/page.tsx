import { AppShell } from "@/components/layout/app-shell";

export default async function OnboardingPage() {
  return (
    <AppShell
      title="Onboarding"
      description="This shell will become the multi-step onboarding flow for accepted members."
    />
  );
}
