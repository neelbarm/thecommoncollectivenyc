import { AppShell } from "@/components/layout/app-shell";

export default async function CohortPage() {
  return (
    <AppShell
      title="Cohort"
      description="Your small-group space for recurring connection, introductions, and shared plans."
    />
  );
}
